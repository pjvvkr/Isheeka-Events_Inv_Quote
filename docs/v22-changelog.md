# Isheeka ERP v22 — Running Changelog & Decision Log

**Purpose:** the single durable record of every change shipped, every database migration run, and every open item — so nothing is lost between sessions. **This is the list we review together before baselining the app.** Claude maintains this; it does not rely on conversation memory.

_Last updated: 13 Jun 2026._

---

## A. Code changes deployed (live on `main` / GitHub Pages)

| # | Change | Notes |
|---|---|---|
| 79 | **Event cancel — choose what happens to the source lead** | Previously cancelling an event left the source lead untouched (only a note added), so a converted lead stayed `event_triggered` even though its event was dead. The cancel modal now has a **Source lead** choice (shown when a lead is linked): **Leave as-is** (default), **Reopen → Contacted** (returns it to the pipeline), or **Mark Lost** (sets stage `lost`, `lost_reason='event_cancelled'`, cancellation reason as the lost note). The dated note is always added regardless. Resets to Leave on each open. Code only — no migration. Transpile-verified (cancel modal sits before the cutoff). |
| 78 | **Quote wizard template dropdown — show all templates, matching first** | The Line-items "Templates" dropdown was filtering to **only** templates whose `event_type` exactly equals the lead's type, so a lead of one type (e.g. Half Saree) couldn't pull a template configured for another (e.g. Engagement) — it showed "No templates for half_saree yet" even though templates existed. Now `getTemplateOptions` lists **all** active non-empty templates: type-matching ones (and sub-event-name matches) surface under **Matching**, everything else under **Other templates**, each labelled with its event type (or [General]). Dropdown headers/empty text updated (uses friendly label, "Other templates"). Code only — no migration. Wizard sits past the bash cutoff → read-back verified. |
| 77 | **Revision history in revised PDFs (quotes + invoices) with include toggle** | `buildQuotationPDF` gained a **"REVISION HISTORY"** section (pink heading, near the end, page-break aware) that renders when `opts.showRevisionHistory` + `opts.revisionHistory[]` are passed. **Invoices** build the rich version from the `invoice_activity_log` `revised` entries (Original total, then each Rev: old→new + reason, by date). **Quotes** reconstruct a thinner version by querying the revision chain (same lead/event, ordered by revision number: version · date · grand total). A **"Revision history" checkbox** appears in the PDF toggles on the quote page and invoice detail **only when revision_number > 0**, **default on**; it flows through download/print/preview and the share-upload paths (`uploadQuotePdf`/`uploadInvoicePdf` gained an `extra` opts pass-through). New state `revHistory`/`includeRevHistory` (quote) and `revRows`/`includeRevHistory` (invoice). Code only — no migration. PDF section + upload helpers verified by read-back (past the bash cutoff); the detail-page plumbing is transpile-validated. |
| 76 | **Quote wizard — auto Valid-until + editable Event date (synced to lead/event)** | **Valid until** now auto-populates: quotation date + 2 days, capped at **event date − 1 day** if that's sooner, never before the quotation date; recomputes when you change the quotation or event date, and **stops auto-recomputing once you edit it by hand** (`valid_until_touched`). Replaces the old settings-`default_validity_days` default. New **Event date (planned)** field added to step 3 (Quote details), seeded from the lead's tentative date / the origin event's main date. On create it **syncs back**: updates the lead's `tentative_date` (lead-origin) and/or the event's `main_date` (event-origin) when changed — so the PDF event date, lead, and event stay consistent. Helper `computeValidUntil(doc,event)`; `event_date` added to the wizard's share `enrichedQuot`. Code only — no migration. Wizard sits past the bash tail-truncation cutoff → verified by read-back. |
| 75 | **Refunds & reversals — client + vendor (bundled with cancellation)** | ⚠ **Run `docs/v22-refunds-migration.sql` first** (adds `is_refund` to `invoice_payments` + `vendor_payments`). A refund is stored as a **negative `is_refund` row** in the same ledger, so every `sum(amount)` nets it automatically. New helpers `recordClientRefund(inv,{amount,reason,date})` and `recordVendorRefund(ev,{amount,reason,date})` insert the negative row and roll back invoice (`total_received`/outstanding/status) or event-vendor (`total_paid`/outstanding/status + installment). **Cancel modal now has two forced-choice sections:** *Client money collected* (per invoice: **Refund to client** with a partial-amount field, or **Retain**) and *Vendor money paid* (per vendor: **Recovered (reverse)** with partial amount, or **Lost — write off**). The "Cancel this event" button is disabled until every client/vendor line is marked; on confirm, refunds/recoveries post automatically, retained/lost stay as-is. **Standalone refund actions** (for refunds that settle later): **↩ Record refund** on the invoice detail (client) and **↩ Refund** per vendor on the event's Vendors section — both with amount/reason/date and guards (refund ≤ collected/paid). **Reports:** cancelled events now use **net retained client money as revenue** (`eventRevenue` switches on status: cancelled → net collected, active → billed fee), so profit = retained − lost vendor − expenses; vendor cost already nets recoveries via the negative rows. App never moves money — records only; transfers happen offline. Verified (logic edits above the bash tail-truncation cutoff). |
| 74 | **PDF — planned event date on cover + header (quotes & invoices)** | Cover page: the line under the event name now prints the **planned event date** (pink Cormorant), replacing the document date; falls back to the doc date if no event date is available. Running page header (right doc box): added an **"Event date"** line **below** Valid-until (quotes) / Due-date (invoices), in pink. Header band given a little more height (`HDR_BOTTOM` 96→108) to fit the extra line. Event date is sourced and threaded into the PDF data: from the linked **event's main date** (invoices + converted quotes) with fallback to the **lead's tentative date** (lead-origin quotes); attached to `quot` on load in QuotationDetail, to `invForPdf` in InvoiceDetail (added `main_date` to those event selects), and to the wizard's `enrichedQuot` share object. Applies to quotations and invoices. Code only — no migration. PDF region verified by read-back (sits past the bash tail-truncation cutoff). |
| 73 | **Fix: Dashboard "Collected this month" ignored cancelled invoices** | The dashboard summed `invoice_payments` purely by payment date, so payments on invoices later cancelled (via event cancellation) still counted — inconsistent with the Invoices module's Received tile, which excludes cancelled. Now the dashboard builds a set of cancelled invoice ids and excludes their payments from "Collected this month" (added `invoice_id` to the payments select). Outstanding already excluded cancelled; quoted-this-month still counts quotes of cancelled events (quotes keep their status by design) — can be revisited if needed. Code only — no migration. Verified (edit above the bash tail-truncation cutoff). |
| 72 | **Quotes flagged when their event is cancelled** | Closes the consistency gap: invoices for a cancelled event become `cancelled` (shown as such + excluded from totals), but quotes keep their stored status (`converted`/`invoiced` — no `cancelled` value in the quote enum). Now derived at display time from the linked event's status: the **Quotations list** shows a red **"Event cancelled"** chip on affected quote rows (alongside status/expired), and the **quote detail** shows a banner ("The event created from this quote (E-…) has been cancelled. Kept for record only.") in place of the usual "event created" banner. `QuotationsModule` loads a set of cancelled event ids; `QuotationDetail` reuses its already-loaded `srcEvent.status`. Stored quote status unchanged — no migration. Verified (edits above the bash tail-truncation cutoff). |
| 71 | **Event cancellation — dependency-aware cancel + reopen** | ⚠ **Run `docs/v22-event-cancellation-migration.sql` first** (adds `cancellation_reason`, `cancelled_at`, `cancelled_by` to `events`; `cancelled` status already existed). Replaces the raw status-flip with a real flow. **⛔ Cancel event** (event detail, hidden once cancelled) → modal showing the **impact summary** (invoices billed/collected, vendor agreed/paid + due installments, expenses, sub-events, lead) + **mandatory reason** + an explicit "on confirm" checklist. **Cascade:** all non-cancelled invoices for the event → `cancelled`; unpaid vendor installments deleted, partly-paid ones keep but `due_date` cleared (kills overdue noise); vendor payments + expenses kept as sunk-cost records; a dated note appended to the source lead (lead left open). Collected money is surfaced as "refund offline" — the app never moves money. A red **cancelled banner** shows the reason on the event. **↩ Reopen event** → status back to Planning + a detect-driven **guided-rebuild banner**: "Regenerate invoice from quote" (when no active invoice + a source quote exists) and per-vendor "Re-add schedule" (agreed amount but no installments); no auto un-void, money records untouched. **Reports** flags cancelled events with a CANCELLED tag and keeps them as sunk-cost losses (fee 0, costs remain). Decisions per plan: allow-with-refund-flag, include-in-Reports, status-only-reopen + guided rebuild, admin/manager intent (role is a placeholder today → effectively all-admin). New EventDetail state/handlers: `showCancel`, `cancelReason`, `doCancelEvent`, `doReopenEvent`, `rebuildRegenInvoice`, `rebuildVendorSchedule`. Verified (edits above the bash tail-truncation cutoff). |
| 70 | **Event types — stronger duplicate validation + clear message** | Settings → Event types add now blocks duplicates by **both** the slugged `value` and a **case-insensitive label** match (previously value-only), checking against active *and* inactive types. The error names the conflict (`"Wedding" already exists.`) and, when the match is inactive, tells you to reactivate it instead of re-adding (`… but is inactive — reactivate it below instead of re-adding.`). Shown in red above the input; clears as you type. Code only — no migration. Verified (edit above the bash tail-truncation cutoff). |
| 69 | **Template items — bulk paste from Excel** | Fast bulk entry on the template editor (no Excel import/upload feature needed). Each sub-event gets a **⎘ Paste from Excel** button opening a paste box; columns auto-detected — **2 cols** `Description ⇥ Qty` add to that sub-event, **3 cols** `Sub-event ⇥ Description ⇥ Qty` build/merge the whole structure across sub-events in one paste (qty optional, defaults to 1). Live "N rows detected / mode" readout before adding. **Also: paste directly into a description cell** — a multi-row clipboard expands into multiple item rows (2-col semantics), replacing the cell row and appending the rest. Handles arbitrarily many rows (plain textarea/paste, no file round-trip). New TemplateEditor helpers `parsePasted`, `applyBulkPaste`, `handleCellPaste`; `TplDescInput` gained an `onPaste` prop. Code only — no migration. Verified (edits in TemplateEditor, above the bash tail-truncation cutoff). |
| 68 | **Template save error now shows the real reason + DB fix for custom types** | The template save catch showed a generic "Could not save template. Please try again." — now it surfaces the actual error (`err.message/details/hint`). Root cause of the save failure on custom event types: `event_templates.event_type` still carried an old CHECK constraint allowing only wedding/corporate/birthday/anniversary/other (the `events` equivalent was dropped earlier; templates had their own, never exercised until the dropdown was unhardcoded in #67). **Fix migration `docs/v22-event-templates-type-check-fix.sql`** inspects + drops any CHECK on `event_templates.event_type` so it's governed by the configurable `event_types` list like leads/events/quotations. Code change is display-only; the DROP is the actual fix. |
| 67 | **Template editor event-type dropdown now uses configurable list** | The Template editor's "Event type" select was hardcoded to 5 values (wedding/corporate/birthday/anniversary/other), so event types added in Settings (Half Saree, Dhoti Ceremony, Baby Shower, etc.) didn't appear and you couldn't build templates for them. Swapped the hardcoded `typeOpts` for the shared `useEventTypes()` hook (same source the Lead and Event forms already use, reading the `event_types` table) — the dropdown now lists every active configured type and picks up future additions automatically. Completes the loop: configure types → make a template per type → import by type in the quote wizard. Code only — no migration. Verified (edit at line ~399, well above the bash tail-truncation cutoff). |
| 66 | **Remove paid vendor → auto-void payments with audit trail** | ⚠ **Run `docs/v22-vendor-payment-void-migration.sql` first** — adds `is_voided` (default false), `void_reason`, `voided_at`, `voided_by` to `vendor_payments` (soft-void; voided rows stay as the audit record). Replaces the old hard block: Removing a vendor that has payments now opens a **mandatory-reason modal** ("Void & remove") → marks all that vendor's payments `is_voided` with the reason/timestamp/user → zeroes the engagement's live totals → soft-deletes it. Installments are kept (so the voided payment rows keep their FK). **Audit trail shown in three places:** (1) **Vendor Payments module** — a **Voided** toggle in the filter bar switches the list to voided payments, each struck-through with reason + voided-by/when; live metrics (paid this month/year) now exclude voided. (2) **Event detail** — a "Voided vendor payments (audit)" line under Vendors & payments listing amount/date/reason for that event. (3) **Reports** — voided amounts excluded from event cost/profit, with a "₹X in voided vendor payments excluded" footnote; Excel vendor-payment ledger also excludes voided. Unpaid vendors still remove instantly (no reason needed). New EventDetail state/handlers: `voidedPays`, `removeReasonFor`, `removeReason`, `confirmVoidRemove`. Verified (parser clears all edited regions; bash tail truncation as before). |
| 65 | **Edit / swap / remove a vendor on an event** | Each vendor on the event's Vendors & payments section now has a **✏️ edit** control opening an "Edit vendor on this event" modal. **Edit details:** change the **service** and **agreed amount** (agreed change recomputes outstanding = agreed − paid, and syncs the lone auto-installment if no payments yet). **Swap vendor:** a dropdown reassigns the engagement to a different active vendor (excludes vendors already on the event); **locked once any payment exists** (a swap would misattribute payment history — message points to remove + re-add). **Remove vendor:** a red Remove button soft-deletes the `event_vendors` row and hard-deletes its (unpaid) installments; **blocked if any payment is recorded** against the vendor (would orphan history). Previously the only writable thing on an engagement was the payment rollup — these were the gaps for "I picked the wrong vendor / scope changed." New EventDetail state/handlers: `editEngage`, `engForm`, `openEditEngage`, `submitEditEngage`, `removeEngage`; vendor-master list now also loads when the edit modal opens. Code only — no migration. Verified (parser clears all edited regions; bash sees a truncated cloud-synced tail as before). |
| 64 | **Vendor installment schedule + overdue + per-installment Pay** | ⚠ **Run `docs/v22-vendor-installments-migration.sql` first** — adds `label` + `when_text` to `vendor_installments` (both nullable; `due_date`/`percentage`/`amount_due`/`amount_paid`/`balance`/`status` already existed). On the event's **Vendors & payments** section each vendor row is now **expandable** into its payment schedule: per-installment **label, due date, amount, balance, status**. **Overdue is computed** (due_date < today AND balance > 0), shown as a red badge on the installment and an **OVERDUE** flag on the vendor row — no stored-status/enum change (mirrors how invoice overdue works); "due soon" within 5 days. **Add / edit / delete installments** inline (label + amount + due date), with a live "scheduled total vs agreed" hint; delete is blocked once an installment has payments. **Per-installment "Pay"** opens the record-payment modal **pre-filled with that installment's balance** and tied to that installment (overdue ones get a pink Pay button); the vendor-level "+ Pay" still works against the default installment. New globals `vendorInstBalance`, `isVendorInstOverdue`, `isVendorInstDueSoon`; `recordVendorPayment(ev,pay)` now honours `pay.installmentId`; EventDetail loads `vendor_installments` per event-vendor. **Pending:** cross-event "Dues" rollup in the Vendor Payments module + feeding Reports outstanding-by-due-date (fast-follow). Verified by parse up to the file tail (bash sees a truncated cloud-synced copy; edits all sit before the cutoff). |
| 63 | **Vendor form: phone/email UX + duplicate check** | **Phone 1** is now **mandatory**, prefilled with `+91 `, and the caret drops to the end on focus; save rejects anything that isn't a valid 10-digit number. **Email 1** prefills `@gmail.com` only when the field is empty (caret lands before the `@`), and is fully overwritable for non-Gmail addresses; a bare `@gmail.com` (or blank) is saved as null. **Duplicate check on create:** before inserting a new vendor, it matches existing vendors on **normalized phone OR exact name** and, if found, shows an inline amber banner — “A vendor "X" already exists” — with *Open it* (jumps to that vendor), *change the details*, or *add anyway* (override). Edits and the existing-vendor picker are unaffected. The `save()` button now calls `save()` (not passing the click event) so the dup guard isn't bypassed. Code only — no migration. Transpile-verified. **(Vendor installment due-dates / overdue + per-installment "Pay" is the next build — pending the `vendor_installments` schema check in `docs/v22-vendor-installments-schema-check.sql`.)** |
| 62 | **Reports/Expenses — Phase 4: PDF + Excel exports** | Two export buttons in the Reports header (next to the range selector), honouring the selected range (Month/Quarter/Year/All). **Excel (SheetJS, already loaded):** a workbook with four sheets — **Summary** (KPIs: billed, collected, outstanding, expenses, vendor payments, event-profit sum, overhead, net profit), **Per-Event P&L** (ref, event, date, client, fee, expenses, vendors, profit, margin + totals row), **Expenses** ledger (date, category, event/General, amount), **Vendor Payments** ledger (date, event, amount); amounts written as raw numbers so they're spreadsheet-usable. Filename `Isheeka_Report_<Range>_<date>.xlsx`. **PDF (jsPDF + autotable, reusing the existing libs):** pink header band with company name + "Performance Report" + range/date, a KPI summary grid, the locked-model footnote, and the per-event P&L as a pink-themed autotable with a highlighted totals row. Filename `Isheeka_Report_<Range>_<date>.pdf`. Both pull `company_name` (PDF also phone/email/website) from settings at export time; both reconcile to the on-screen numbers. Code only — no migration. Transpile-verified. **This completes the Reports/Expenses plan (Phases 1–4).** |
| 61 | **Dead-code cleanup (post quote-page refactor)** | Removed code left unreferenced since the Phase-1/2 quote-page refactor. **Deleted `ClientMatchModal`** (only mount was the now-removed in-lead convert flow). **`QuotationDetailModal`** (replaced by the `QuotationDetail` page; zero mounts) collapsed to an inert stub. **LeadDetail:** removed the dead in-lead conversion internals — `handleConvertInitiate`, `handleConvert`, and the `ConvertLeadModal`/`ClientMatchModal` mounts (lead-origin conversion now happens on the quote page via `createEventFromQuote`; `showConvert` was never set true in the lead). Removed the unreferenced `showConvert`/`viewQuotId`/`clientMatches` state in LeadDetail and the unused `viewQuotId` state in EventDetail. **Kept** `ConvertLeadModal` (still live on the quote page) and the welcome-flow state (harmless). No functional change. Transpile-verified; residual-symbol grep confirms no remaining mounts. |
| 60 | **Fix: Vendors form lost focus after each keystroke** | The new vendor add/edit modal in `VendorsModule` had its `FormModal` declared as a component *inside* the module, so every `setForm` created a new component type and React remounted the inputs each keystroke — focus jumped out after one character. Changed it to plain inline JSX (`formModal`), matching the other modules' modals. No behavior change otherwise. Transpile-verified. |
| 59 | **Reports/Expenses — Phase 3: Reports dashboard** | New **`ReportsModule`** (sidebar **Reports** stub → real), routed at `activePage==='reports'`. **Range selector** (Month / Quarter / Year / All), default Year. **KPI cards:** Billed (fee), Collected, Outstanding (current, across all open invoices), Expenses + vendors, and **Net profit** — all range-filtered (cash KPIs by doc/payment/expense date). **Net profit uses the locked model:** per-event profit = event fee (Σ non-cancelled invoice grand totals) − event expenses (`expenses.event_id`) − event vendor payments (`vendor_payments.event_id`); period net = Σ(per-event profit for events whose **`main_date`** falls in the range) − Σ(general/overhead expenses dated in range). **Charts (dependency-free inline SVG/CSS, no new libraries):** Revenue-collected last-12-months bars; Income-vs-cost last-12-months grouped bars (billed vs expenses+vendors). **Pipeline by stage** (active leads collapsed to New / Contacted / Quoting, count + Σ budget, with potential total). **Upcoming events** (next 8, click → event quick-view popup). **Profitability-by-event table** for the range (Fee / Expenses / Vendors / Profit / Margin per event, click row → quick-view), with a totals row and a period-net line that surfaces the overhead deduction. Reuses `EventQuickView`, `fmtDate`, `leadStageDisplay`. Reads `invoices`, `invoice_payments`, `expenses`, `vendor_payments`, `events`, `leads`. Code only — no migration. Transpile-verified. Next: Phase 4 (PDF/Excel exports). |
| 58 | **Vendors master module + pick-existing wiring** | New **`VendorsModule`** (sidebar **Vendors** stub → real), modeled on Clients. **List:** metric cards (Total / Active / Preferred / Inactive), search (name, contact, phone, GST, city), category + status filters, rows with avatar, preferred ★, category chip, status pill (inactive rows dimmed). **Detail/edit:** all `vendors` fields — name, category, contact_person, phone 1–3, email 1–3, street/city/state, GSTIN, PAN, bank name/account/IFSC, UPI, payment terms, rating (0–5), preferred flag, status, notes — via a full add/edit modal; status active/inactive switch; **Engagements** section listing this vendor's `event_vendors` (event ref click → opens the event, agreed/paid/balance per row). **Soft-delete is blocked when the vendor is on any event** ("Set them Inactive instead"), exactly like the Clients rule; otherwise archives (`is_deleted`). Routed at `activePage==='vendors'` (excluded from ComingSoon). **Pick-existing wiring (no more duplicate vendors):** the event's **"Vendors & payments → Add vendor"** modal now toggles **Pick existing / + New vendor** — "Pick existing" lists active master vendors not already on the event (auto-fills category); the Vendor Payments module's **+ Record payment → Add a new vendor** branch gained a **"From vendor master"** dropdown (active vendors not already on the event) with "— Create brand-new —" as the fallback. **Inactive vendors are excluded** from both pickers. Code only — no migration (the `vendors` table already exists with all fields). Transpile-verified. |
| 57 | **Reports/Expenses — Phase 2: vendor payments (event-tied)** | Schema confirmed via `v22-reports-expenses-schema-check.sql`: `expenses` columns match Phase 1 exactly (no change); **`vendor_payments.installment_id` is NOT NULL** → handled without migration by auto-creating one default `vendor_installment` (= agreed amount) per event-vendor and tying payments to it. **Shared helpers** `addEventVendor` (creates `vendors` row if new + `event_vendors` + default installment), `recordVendorPayment` (inserts `vendor_payments`, rolls up the installment + `event_vendors` total_paid/outstanding/status). **Per-event "Vendors & payments" section** on the event (replaces the old placeholder): add vendor (name/category/service/agreed), per-vendor **+ Pay**, with a "Vendor cost: paid / outstanding / agreed" summary. **`VendorPaymentsModule`** (sidebar stub → real): searchable cross-event list — Date, Vendor/service, **Event ref (click → event quick-view popup)**, mode, amount; metrics (paid this month/year, outstanding to vendors); filters (event, date); **+ Record payment** (select event → pick an event-vendor or add a new one → amount/date/mode/ref). Both views write the same `vendor_payments` (in sync) and feed per-event cost for Reports profitability. New `EventQuickView` popup. (Cross-event **Vendors** master module still a stub — fine for now.) Event pickers (expense form + filter, vendor-payment dropdowns) now show **ref · name · client · date** so the right event is easy to identify when tagging a spend. Code only — no migration. Transpile-verified. |
| 56 | **Reports/Expenses — Phase 1: Expenses module** | Sidebar **Expenses** stub → real module. Metric cards (this month / year / event-linked / general), search (description/reference/sub-category), filters (category, event vs general, date range), and an add/edit modal: description, amount, date, **category** (8-value enum), sub-category, **link-to-event** (blank = general/overhead), payment mode (cash/UPI/NEFT/cheque), reference, **recurring** toggle + frequency, **receipt upload** (storage `quotations` bucket, `receipts/expenses/`), notes. Rows show the linked event ref (click → opens the event) or "General"; edit + soft-delete. Writes the existing `expenses` table; `created_by` via `_currentUid()`. **Run `docs/v22-reports-expenses-schema-check.sql` to confirm the live `expenses` columns match** (built to `v22-db-schema.md`; will adjust if the check shows differences). Next: Phase 2 (vendor payments — needs the schema-check result, esp. `vendor_payments.installment_id`). Code only — no migration. |
| 55 | **Cover typography — Cormorant Garamond (pink) + client-box fix** | **Client box:** phone &amp; email now share one line separated by " | "; card height raised so the city no longer clips the border (Event card too). **Cover font:** the tagline, date, "Dear …" greeting, intro paragraphs, "With warm regards,", "Swathi", and "Founder &amp; CEO" are now **Cormorant Garamond bold-italic in brand pink (#D4537E ≈ PINK)**. The wordmark and "Since 2017" stay Great Vibes script; "PREPARED FOR" + event name stay pink sans. **Font delivery:** Cormorant Garamond (bold-italic) is fetched once at app start from jsDelivr (Google Fonts TTF) and registered with jsPDF (`_CG_B64` → `addFileToVFS`/`addFont`); a new `cormorant()` helper applies it, **falling back to Times-italic** if the font hasn't loaded (offline / first PDF before fetch completes) — so the PDF never breaks. The header-band tagline stays small grey italic (running header kept understated). Code only — no migration. |
| 54 | **PDF template polish (quote + invoice)** | Visual fixes to the shared `buildQuotationPDF`, so both documents get them. **Cover:** extra line gap between the tagline and "Since 2017"; **event name now in pink** (was black); a line gap between "PREPARED FOR" and the event name. **Line-items table:** added a **pink outer border** (`tableLineColor`/`tableLineWidth`) over the existing grey grid. **Client + Event cards:** outline changed to **pink**. **Summary never orphaned:** the pricing summary + payment schedule (and the grand-total bar) will **never start alone at the top of a page** — the table now reserves space for the summary block at the page bottom, so autotable breaks earlier and pulls the last item rows onto the same page that carries the summary. The reserve is sized to the actual block (installment count, GST/discount/received lines), so short quotes are unaffected. Code only — no migration. (PDF region sits past the bash-mount truncation, so verified by read-back rather than transpile.) |
| 53 | **Status lifecycle: event auto-progression + Workflow section + overdue/expired flags + lead-stage labels** | From the status-lifecycle review. **(1) Event auto-progression** (was fully manual): issuing an invoice (Mark sent) advances its event `planning → confirmed`; on opening an event past its `main_date` it advances to `in_progress` (forward-only; never touches completed/cancelled). The events **list** shows the effective status too (past-date events read In progress) via a new `effectiveEventStatus()` helper. **(2) Event "Workflow" section:** the event's **Documents** card is renamed **Workflow** and now leads with the **🎯 Lead → 📄 Quote(s) → 🎪 Event → 🧾 Invoice(s)** chain (each clickable); the **← Lead** link moved out of the header into this chain. **(3) Invoice "overdue" now auto-sets:** an issued/partly-paid invoice past its `due_date` flips to `overdue` (written on invoice open, shown live in the invoices list via `isInvoiceOverdue()`). **(4) Quote "expired" flag:** a sent quote past `valid_until` shows an **Expired** badge on the quote page + list — **display only**, it does *not* change the stored status or block Revise/Confirm (so you can still act on a just-expired quote). **(5) Lead stage labels collapsed:** the lead chip now shows **Quoting** for the various quote sub-stages (`leadStageDisplay()`), so the pipeline reads New → Contacted → Quoting → Converted → (Lost/Completed); the stored stages and the list filter are unchanged. New helpers: `todayLocalStr`, `effectiveEventStatus`, `isQuoteExpired`, `isInvoiceOverdue`, `leadStageDisplay`. Code only — no migration (all statuses already in the CHECK enums). Transpile-verified. |
| 52 | **Phase 2 (steps 2–5) — quote page owns confirm/convert; lead slimmed** | **Quote page Confirm is now origin-aware:** event-origin → "✅ Confirm & create invoice" (approve + draft invoice, as before); **lead-origin → "✅ Confirm & create event"** → opens the `ConvertLeadModal` and runs the shared `createEventFromQuote` (client → event → sub-events → mark quote converted → draft invoice → link lead) → lands on the new event. Idempotent: an already-`approved` lead-origin quote can still be converted (handles leads confirmed under the old flow). **Lead detail slimmed:** the five quote-stage buttons (Continue quote, Revision requested, Generate revised quote, Quote confirmed, Convert to event) are gone; once a quote exists the lead shows a single **📄 Open active quote →** deep-link. The lead keeps Mark contacted, **Generate (first) quote** (only at the `contacted` stage), Lost, Edit, and the stage chip. **Event detail:** the active-quote **✏️ Revise** button became **📄 Open quote →** (revise now lives only on the quote page); "+ Create" (first quote from an event) stays. **Dead code:** the lead's convert internals (`handleConvert`/`ConvertLeadModal` mount/`ClientMatchModal`) and the unused `QuotationDetailModal` are now unreferenced — left in place to avoid a risky large deletion; flagged for a later cleanup pass. The `quote_revision_pending` / `quote_confirmed` stages are no longer set by buttons (revise/confirm happen on the quote page) — harmless; existing rows still render and route correctly. Code only — no migration. Transpile-verified. |
| 51 | **Phase 2 step 1 — extract `createEventFromQuote` (refactor, no behavior change)** | The lead→event conversion logic (client resolve → event → sub-events → mark quote converted → draft invoice → link lead) moved out of `LeadDetail.handleConvert` into a module-level `createEventFromQuote(lead, opts)` helper. `handleConvert` is now a thin wrapper that calls it and handles the UI (welcome modal, reload). **No functional change** — same conversion, same result. This is the foundation so the quote page can run the same conversion on "Confirm" (Phase 2 step 2). Verify the lead→event conversion still works after pushing. Code only — no migration. |
| 50 | **Block create when installments ≠ grand total (proactive)** | The create handler already *rejected* an unbalanced schedule with a toast (#45). Made it proactive: the **Create quotation / Create revised quote button is now disabled** while the installments don't sum to the grand total, with an **inline reason** beside it ("⚠ Installments exceed by ₹X — must total ₹Y" / "₹X short"). Applies everywhere the wizard runs (lead, event, quote page). Code only — no migration. |
| 49 | **Quotation Detail page (Phase 1) — quotes get a real action-hub page** | New `QuotationDetail` **full-page** component (replaces the read-only `QuotationDetailModal`), carrying every modal capability **plus editing**: header (ref · rev · status · client·event·dates) + **source chain** (Lead → Quote → Event, clickable); action bar with **✏️ Revise / Edit quotation** (new), **✅ Confirm & create invoice**, **Go to event**; the **"What the client sees"** card (Full/Items/Summary presets + the seven toggles: Prices, Quantities, Sub-event grouping, Payment schedule, Discount, Cover page, Bank details); **Share/export** row (WhatsApp / Gmail / Email / Print / Download — all upload the branded PDF + log the send); line items grouped by sub-event + totals; payment schedule (amounts) + terms; collapsible **Activity & change log**; status banners. **Revise/Edit** reconstruct the wizard context from the quote's origin — event-origin builds the event+client adapter, lead-origin loads the lead — and open `QuoteGenerationWizard` (draft → edit in place; issued → new revision). After a revision the page jumps to the new revision. **Gating:** Revise/Edit hide when the quote is superseded/rejected/expired/converted **or** an invoice has been issued for its event (shows "🔒 Invoice issued — revise the invoice"). **Routing:** the Quotations module list now opens the page (not the modal); quote rows on the **lead** and **event** now navigate to the page (`onNavigate('quotations',{quotId})`); the invoice source-quote link already did. All three modal mounts removed. _Hybrid (per plan): the lead/event stage buttons keep their own in-place wizard for now — Phase 2 converts those to deep-links and deletes the dead modal component + unused `viewQuotId` state._ Code only — no migration. Transpile-verified. |
| 48 | **Quote share link + footer details + revision event-name + step-4 Close** | Four fixes from testing the revised-quote flow. **(1) Footer showed a hardcoded website** (`www.isheekaevents.in`) instead of the one in Settings — the quote-PDF `settings` SELECTs (modal print/download, wizard PDF + share) only fetched bank/UPI fields, so `phone_1`/`email`/`website` fell back to hardcoded defaults. Added them (and `company_name`) to those selects, so the footer now uses the real Settings values. **(2) WhatsApp/Email share from the wizard's step-4 had no quotation link** — that path built its own bare message and never uploaded a PDF, unlike the quote-detail modal. Reworked both wizard handlers to **upload the branded PDF to storage and use `buildQuoteShareMsg`** (the same link + contact-footer message the modal uses). **(3) Revised quotes kept the old `{Client}'s {Type}` event name** — the revision loaded the prior quote's stored name. Now revisions default the event name to the **`{Type} Event`** formula (editable in step 3), consistent with new quotes. _(Applies to newly generated revisions; an already-created quote keeps its stored name until re-revised or edited.)_ **(4) The step-4 "Revised quotation created!" screen had no way out without "Mark as sent"** — added a **"Close — I'll mark it sent later"** button (closes + refreshes; the quote stays a draft). Code only — no migration. |
| 47 | **Repeatable quote revision from a lead** | At the **"Revised Quote Sent"** lead stage the only action was **✅ Quote confirmed** — there was no way to start another revision round from the lead (you'd be stuck after the first revised quote). Added a **🔄 Revision requested** button to that stage (mirroring the one on "Quote Sent"), which moves the lead back to "Revision Pending" → then **📋 Generate revised quote** opens the wizard pre-loaded with the active quote. Revision is now a repeatable cycle from the lead, not a one-shot. Code only — no migration. |
| 46 | **UPI ID on PDF fix + revision payment-terms auto-update** | **(1) UPI not printing:** the PDF bank-details line already had UPI logic, but the four `settings` SELECTs that feed the PDF (invoice detail, quote-detail modal print/download, quote wizard share) only fetched `bank_name/account_number/ifsc_code` — **`upi_id` was never selected**, so it was always blank in the PDF even when set in Settings. Added `upi_id` to all of those selects. **(2) Revision payment-terms didn't auto-update:** when revising a quote, the loaded quote's existing terms set `payment_terms_touched=true`, which suppressed regeneration — so editing installment amounts didn't refresh the terms text. Changed the revision/continuation load to keep `payment_terms_touched=false`, so the terms text now regenerates live as you change amounts (and only "sticks" once you hand-edit the terms box). _Note: the `{Type} Event` event-name default + editable event-name fields (changelog #44) are confirmed present in code — they ship once these batches are pushed._ Code only — no migration. |
| 45 | **"Since 2017" + amount-based payment schedule with % calculator** | **(1)** PDF wordmark tagline changed from "Since 2010" → **"Since 2017"** (header band + cover). **(2) Payment schedule now takes amounts, not percentages.** In the quote wizard each installment is an **Amount (₹)** field with a small **"%" calculator** button (type a percentage → it fills `amount = that % of the grand total`, showing the math), plus a live **"Allocated ₹X of ₹Y / Unallocated / Over by"** indicator that turns green when balanced. **Validation:** creating/continuing a quote is blocked unless the installments sum to the grand total (items − discount). Amounts auto-fill from the default 50/40/10 ratios until you edit one (`payment_schedule_touched`), and proportionally re-fill as the total changes; revisions preserve the prior amounts. **Data:** `payment_schedule` now stores `{amount, pct, label, when}` — amount is the source of truth, the derived `pct` is retained so the invoice's **GST-toggle / revise rescaling still works** (proportional). Invoice installments are created from the **exact amounts** (falling back to pct × grand for older quotes). **Auto payment-terms text** and the **PDF payment-schedule block** + quote-detail modal now show amounts ("₹56,140 (Advance) due on confirmation; …") instead of "50%". **(3)** Removed the unused/decorative "Default payment schedule" preview card in Settings (it was a hardcoded literal, not wired to anything). Code only — no migration (`payment_schedule` is freeform JSON; old pct-only quotes still render via the pct fallback). |
| 44 | **Event name: new "{Type} Event" default + editable on quote & invoice** | The document **event name** (shown on the PDF cover "PREPARED FOR" and the EVENT card) now defaults to **`{Event Type} Event`** — e.g. "Half Saree Event", "Wedding Event" — via a `defaultEventName()` helper, replacing the old `"{Client}'s {Type}"` auto-name. Per the decision, this default applies to **all** new quotes (lead- or event-originated). **Editable in two places:** (1) the **quote wizard** details step gains an **"Event name"** field, pre-filled with the default (revisions pre-fill from the prior quote's name), saved to `quotations.event_name` and used everywhere the quote name shows; (2) the **invoice detail** gains an inline **"Event name"** field (since invoices are auto-created with no generate form), pre-filled from the quote's snapshot, saved to `invoices.event_name` and reflected on the invoice PDF — shown only while the invoice is editable (hidden when paid/cancelled, like the other edit controls). The lead→event conversion's auto event name uses the same `{Type} Event` fallback. Code only — no migration. |
| 43 | **Date off-by-one fix + event-type label display** | **(1) Timezone date bug:** date-only fields (tentative date, follow-up, event main date, sub-event dates, valid-until, doc date, due date, payment date) displayed one day early in timezones behind UTC, because `new Date("YYYY-MM-DD")` parses as UTC midnight and then shifts when formatted locally. Added a timezone-safe **`fmtDate()`** that builds bare `YYYY-MM-DD` values from their parts as a local date (full timestamps fall through unchanged), and routed every date-only display through it — including the three local `fmt` helpers (quote modal, invoice detail, PDF builder) and ~15 inline sites across lead detail/list, event detail/list, quote/invoice lists, dashboard, installment schedule, and the WhatsApp/email message builders. Stored data was always correct; this was display-only. Timestamps (Created/Converted) were already correct and left as-is. **(2) Event-type label:** multi-word custom types showed their raw value (e.g. `Half_saree`) instead of the label ("Half Saree") because display code printed the stored `event_type`/`type` value with a naive capitalize. Added **`eventTypeLabel(value)`** — looks up the configured label from a cached value→label map (seeded with the defaults, refreshed by `fetchEventTypes`), falling back to a prettified value (underscores → spaces, title-cased) so it always reads cleanly even before the list loads. Pointed all ~10 event-type display spots at it (lead detail chip + row, lead/event list rows, event-detail header, template tags, the auto-generated event-name builders, and the WhatsApp/quote messages). Code only — no migration. |
| 42 | **Configurable event types + client deactivate/archive + inactive exclusion** | **(1) Configurable event types** (mirrors the lead-sources pattern): new **`event_types`** table is the source of truth, a new **Settings → "Event types"** tab (🎉) lets you add / rename-by-re-adding / activate-deactivate / reorder, and a cached `fetchEventTypes()` + `useEventTypes()` hook feed the five event-type dropdowns (new-lead form, new-event wizard, event-detail edit, and the Leads & Events list type-filters) — all previously hardcoded to the fixed five. Custom labels auto-generate a lowercase value (e.g. "Baby Shower" → `baby_shower`). Also fixed the Events list type-filter, which compared a capitalized value against lowercase-stored `events.type` and never matched. **Requires migration `docs/v22-event-types-migration.sql`** (creates the table, seeds the current five, **drops `events_type_check`** so custom types are allowed — same approach as the old `leads_source_check` drop). **(2) Client status + delete:** the client detail header now has a **status control** (Active / Inactive / VIP) and a **🗑 Delete**. Per the decision, Delete is a **soft archive** (`is_deleted=true`, recoverable in DB) and is **blocked when the client is linked to any non-deleted lead/quotation/event/invoice** — it counts references and tells you to set Inactive instead. **(3) Inactive exclusion:** **inactive** (and archived) clients are filtered out of every new-record client picker — change-client modal, new-event wizard, quote wizard — and out of the lead→event phone dedup-match and the quote-wizard duplicate check, so an inactive client can't be attached to anything new. They remain visible/filterable inside the Clients module; existing linked documents are untouched. Code verified via isolation transpile. |
| 41 | **Email-validation trim bug + quote-revise gated post-issuance (Option A)** | **(1) Email validation bug fix:** valid emails (e.g. `p.Vamsy@gmail.com`) were rejected when the field had an invisible leading/trailing space (common from autofill/paste/mobile keyboards) — the regex `^[^\s@]+…$` correctly disallows surrounding whitespace but the validator tested the raw, untrimmed value. Now **all email checks `.trim()` before testing** (ClientForm `email_1`, Settings/company `email`, CSV bulk-import `email_1`), optional **`email_2`/`email_3` are now validated too** (with their inline error display wired up), and **ClientForm save trims** all contact fields (names, emails, phones, address) so a stray space is never stored. **(2) Quote-revise gated after invoice issuance (Option A):** on the event's Documents card, the quote **✏️ Revise** button now hides once a **non-cancelled invoice has been issued** (`sent`/`partially_paid`/`paid`/`overdue`), replaced by a muted **🔒 "Invoice issued — revise the invoice"** hint. Rationale: pre-issuance the quote is the master (and a *draft* invoice still auto-refreshes from it, so Revise stays then); once issued, the invoice is the frozen master and scope/pricing changes go through *invoice* revise (paid invoices are fully locked). Closes the hole where revising a quote behind a paid/sent invoice silently diverged the two documents. The lead-detail revise path is pre-conversion (no invoice yet) and the quotation modal has no revise button, so this is the only affected entry point. Code only — no migration. |
| 40 | **Paid-cascade + source Lead→Quote→Event links + mark-event-completed** | Three linked changes from the post-payment lifecycle review. **(1) Source chain on invoice detail:** the old single "Source quotation" line is now a **Lead 🎯 → Quote 📄 → Event 🎪** breadcrumb, each clickable to navigate to that record (lead resolved from the invoice's quote when the invoice has no own `lead_id`; event from the invoice's own `event_id` else the quote's). **(2) Full-payment cascade** in `recordPayment`: when a payment clears the balance (`total_received ≥ grand_total`), the invoice goes `paid` **and** (a) every installment is reconciled to `balance:0, status:'paid'` — fixes the old "invoice paid but Balance installment still pending" divergence — (b) the **source quotation is marked `'invoiced'`**, and (c) a `paid_in_full` activity entry is logged. A **paid invoice is now locked for edits** (✏️ Revise hidden when `paid`/`cancelled`; GST toggle already gated; **✏️ Edit client also hidden when locked** — added after testing, since it edits the client master and an edit affordance on a locked invoice was confusing; client contact edits remain available from the Clients module / event / lead), with a green "fully paid and locked — sharing & printing remain available" banner. **(3) Mark event completed:** EventDetail gains a **✅ Mark completed** action (hidden once completed/cancelled) — sets `events.status='completed'` **and** closes the source lead (`leads.stage='completed'`), behind a confirm that names the lead. Deliberate design: payment ≠ delivery, so lead/event closure ties to **event completion**, not payment (per the lifecycle discussion). **Requires the enum migration `docs/v22-invoice-paidcascade-migration.sql`** (adds `quotations.status='invoiced'` and `leads.stage='completed'`). Code verified via isolation transpile. |
| 39 | **Payment attachment + record-on-draft** | Record-payment modal now takes an optional **payment confirmation** (image or PDF, from phone or PC) — uploaded to the `quotations` storage bucket under `receipts/`, URL saved to `invoice_payments.receipt_url`, shown as a **📎 receipt** link in the payments list. Also loosened the gate so **+ Record shows on drafts** with a balance (advance-on-confirmation), not just Sent+. **Requires migration B-12.** |
| 37 | **Empty due-date hidden + draft auto-refresh (3b-ii #12)** | PDF header no longer prints "Due date: --" when there's no due date (line hidden when empty). **Draft auto-refresh:** when a newer quote is confirmed and the event's invoice is still a **draft with zero payments**, `createInvoiceFromQuote` now refreshes that draft in place from the new quote (same ref, no rev bump), preserving the invoice's GST/discount, and logs an `auto_refresh` entry. Sent/paid invoices are left untouched (snapshot). |
| 38 | **Phase 4 — record payments** | Invoice detail: **+ Record** opens a payment modal (amount, date, mode [UPI/Bank-NEFT/Cash/Cheque → DB enum upi/neft/cash/cheque], against-installment [required], reference, notes). On save: inserts `invoice_payments` (recorded_by resolved to the staff `users.user_id`), updates the installment's amount_paid/balance/status, recomputes invoice `total_received`/`total_outstanding`/`status` (partially_paid/paid), and logs a `payment` activity entry. Payments list shown on the detail; dashboard **"Collected this month"** now populates. ⚠️ Money-write code authored without a live preview — **test carefully**. _A1 (installment due-dates + overdue auto-status) still pending — follow-up._ |
| 36 | **PDF cover tweaks + invoice PDF options** | Cover: reduced logo height (120→104) + more space before the wordmark; "PREPARED FOR" now shows the **event** (date below) instead of the client name (name's already in the "Dear …" greeting). Invoice detail gains an **"Include in PDF"** toggle row (Prices / Qty / Schedule / Discount / Cover page / Bank details) — same flexibility quotes have; wired into Download/Print/Preview/Email/WhatsApp. |
| 35 | **PDF redesign v2 — logo + dedup + Total + multipage + spacing** | Replaced the embedded logo with the new **icon-only** mark (transparent PSD → flattened to white → PNG, 8.8 KB; aspect-preserved, ~46 pt header / ~120 pt cover). Branding **de-duplicated** (logo no longer carries text, so the script "Isheeka Events" + tagline + "Since 2010" beside it is the only brand text). **"Since 2010"** shrunk (header 11 pt, cover 15 pt). Grand total now labeled **"Total"** for quotes (stays "Grand total" for invoices) and **always renders** even when prices toggle is off. **Fixed the broken multi-page continuation header** (full header now repeats cleanly on every page via a consistent top margin — no more page-3 overlap). Added **breathing room**: bigger gaps after cards + table, payment details pushed down, summary/schedule/balance spaced. Quote price/qty toggles unchanged. File ~40 KB smaller (new logo vs old JPEG). |
| 33 | **PDF redesign (quote + invoice) — branded layout + script font** | Rewrote `buildQuotationPDF`: header band (script "Isheeka Events" wordmark + tagline + script "Since 2010", ref/date/due-or-valid + status), **Client** + **Event** cards, pink-header line-items table grouped by sub-event (respects prices/qty toggles), **Summary** box with pink grand-total bar + GST line (invoices) + Received/Balance, payment schedule, payment details, terms, footer with page numbers. **Cover page** (only when the toggle is ticked): centered logo, script wordmark/Since-2010, doc pill, "PREPARED FOR", and an **editable personalized intro** from `settings.cover_intro` with `{client}`/`{event}` placeholders, script greeting, and **"Swathi"** sign-off. **Great Vibes** font embedded as base64 (`addFileToVFS`/`addFont`) for all script text; falls back to Times-italic if it fails. Multi-page handled by autotable (header/footer repeat). Shared engine via `docType`. ⚠️ Drawn blind (no jsPDF preview in dev) — expect a look-and-tune cycle on real output. |
| 34 | **G1/G2/G3 (test-found)** | G1: quote-detail Activity section collapsible (collapses when >1 entry). G2: list/search now sorts active/relevant first, dead (superseded/rejected/expired/cancelled/lost) last, newest within each — Leads, Quotations, Invoices (Events already status-ordered). G3: invoice detail shows a clickable **Source quotation** (ref · status · total). |
| 32 | **QA fixes C3 + C5** | C3: Dashboard "this month"/"upcoming" date checks now use local `YYYY-MM`/`YYYY-MM-DD` string compares (no UTC `new Date()` drift near month boundaries). C5: Revise modal validates the discount (0 ≤ discount ≤ subtotal) before saving. A1/A2/A3 deferred into Phase 4 (payment-status logic — untestable until payments exist); B1/B2 into the post-baseline RPC pass; C1 is an inherent caveat (can't confirm delivery). Code only. |
| 31 | **Phase 3b-ii — revise invoice + change log** | Invoice detail gains **✏️ Revise** (any non-cancelled invoice): a modal to edit line items + invoice-level discount, with a **mandatory reason**. On save it recomputes subtotal/GST/grand, rescales installments, replaces line items, and writes an `invoice_activity_log` "revised" entry (old→new grand total + reason) that appears in the Activity & change-log timeline. `revision_number` bumps **only if the invoice has been issued** (Sent+); drafts update in place. **Paid-in-full** invoices can be revised behind a stronger confirm (admin action; the role gate binds once real roles land per P0-3). Code only — no migration. **Remaining 3b-ii sub-item: draft auto-refresh** (decision #12) — isolated follow-up as it touches the `createInvoiceFromQuote` money-path. |
| 30 | **Dashboard — live data + operational widgets** | Was a static placeholder (hardcoded 0s + first-run onboarding). Now the 4 metric cards read live: **Active leads** (open pipeline), **Upcoming events** (future, not completed/cancelled), **Quoted this month** (Σ this-month quotes, excl. superseded), **Collected this month** (Σ `invoice_payments` this month — ₹0 until Phase 4, then auto-populates). Cards click through to their modules. Onboarding panel replaced with **Upcoming events** + **Outstanding invoices** lists (clickable to the record). Code only — no migration. |
| 29 | **Standalone Quotations module** | Sidebar **Quotations** is now a real module (was a stub): searchable/filterable list of all quotes with metrics (total / awaiting response / confirmed / draft), status filter, rows open the existing quotation detail modal. Mirrors the Invoices module. Wired `navigate` to carry `quotId`. Superseded rows dimmed. Code only — no migration. |
| 28 | **Phase 3b-i — send/audit trail (activity timelines)** | Every client-facing send now logs an entry: quote sends → existing `quotation_activity_log`; invoice sends → new `invoice_activity_log` (`action='sent'`, `channel`, timestamp, `*_by`). Wired into all three share paths (quote-detail modal, quote wizard share step, invoice detail). **Displays:** invoice detail gains a collapsible **Activity & change log** with a "Sent N× · last sent …" summary; quote-detail modal gains an **Activity** section with the same. Sender resolved to a name via the `users` table. **Requires the `invoice_activity_log` migration (Section B-11).** Quote logging works without migration (table already existed). **Fix:** `*_by` is now resolved to the app `users.user_id` (matched by the logged-in email), not the raw Supabase auth id — the auth id isn't a `users` row and was failing `quotation_activity_log.logged_by`'s FK, silently dropping every quote-send log. Also: quote modal + invoice detail now refresh their Activity list immediately after a send. |
| 27 | **Invoice preview + client details + edit-with-cascade** | Invoice detail gains a **👁 Preview** button (opens the rendered invoice PDF in a new tab; new `action:'preview'` in the PDF engine). New **Client details** card (name, phone(s), email(s), address, GST no.) read live from the `clients` master. **✏️ Edit client** opens the shared `ClientForm` in a modal → updates the `clients` master (contact fields cascade automatically since docs read them live); a **name change is cascaded** to `client_name` on the client's **active** documents only — draft `quotations` / draft `invoices` / non-completed-non-cancelled `events`. Already-sent/finalised documents keep their original name as a snapshot. Code only — no migration. |
| 26 | **Invoice Phase 5 — invoice PDF + share** | Extended `buildQuotationPDF` with `docType='invoice'`: title/pill say "Invoice", header shows **Due date** (not Valid until), totals add a **GST line** (when applicable) + **Received / Balance due**, filename `Invoice_<ref>…`. Invoice detail now has working **⬇ PDF (download)** and **Print** (any status), plus **WhatsApp** and **Email** (only when status is Sent or later). New helpers `uploadInvoicePdf` (Storage → `invoices/…`) + `buildInvoiceShareMsg`; client phone/email enriched from the linked client. Installment schedule rendered in the PDF with amounts + due dates. Added `.gitattributes` (eol=lf) to stop CRLF diff churn. Code only — no migration. |
| 1 | **P0-1 — App error boundary** | Render crashes show a branded "your data is safe" screen instead of a white page. |
| 2 | **P0-2 Phase 1 — toast system + `runDb` + replaced 11 `alert()`s** | Non-blocking branded toasts; central DB-error helper. |
| 3 | **P0-2 Phase 2 — error handling on money-path writes** | Quote wizard + lead→event conversion now surface failures (no silent data loss). |
| 4 | **P0-2 Phase 3 — error handling on remaining writes** | Events detail, templates, lead sources, clients, alt-contacts, checklist; optimistic reverts. |
| 5 | **P1-5 — client contact on re-downloaded PDFs** | QuotationDetailModal enriches client phone/email/city from client→lead. |
| 6 | **Issue #1 — template "saved" toast** | Success confirmation on template save. |
| 7 | **P2-10 — pinned CDN dependency versions** | react 18.3.1, react-dom 18.3.1, @babel/standalone 7.29.7, supabase-js 2.106.2. |
| 8 | **P0-4 — atomic ref-number counters** | `getNext…Ref()` use the `next_counter` Postgres RPC; no duplicate refs. |
| 9 | **Event refs (`E-YY-####`)** | Generated on event creation (wizard + conversion); shown in Events list + detail. |
| 10 | **GST % field in Settings → Bank details** | Configurable rate for invoices. |
| 11 | **#3 — quote↔event link on conversion** | Conversion stamps `quotations.event_id` (+ stores `events.lead_id`). |
| 12 | **Lead → event navigation** | "Converted to event" card on the lead with a "View event →" button. |
| 13 | **Event → lead navigation** | "← Lead" button on the event detail; app navigation extended for `leadId`. |
| 14 | **Context-aware quotation detail** | Status banners (confirmed / event-exists), "Go to event"; quote rows clickable from the event. |
| 15 | **Share: WhatsApp / Gmail / native email + hosted PDF link** | Professional message (matches welcome-message tone); PDF uploaded to Storage; phone validation. |
| 16 | **Readable PDF filename** | `Quotation_<ref>[-rN]_<YYYYMMDD-HHMM>_<code>.pdf`. |
| 17 | **Ref search in lists** | Leads / Events / Clients search now matches their ref numbers; placeholders updated. |
| 18 | **#2 (2a/2b) — quote-from-event** | Wizard `originEvent` adapter; "+ Create" on an event launches it (client pre-filled, links to event, no lead). |
| 19 | **2c — Revise from event** | "+ Create" flips to "Revise" once an active quote exists; revision preloaded + supersedes; lead-wording hidden for event-origin. |
| 20 | **Item carry + sync (quote = master, event mirrors)** | Pre-fills quote from event items; on save rebuilds event sub-events/items to mirror the quote (full mirror). Verified — event total reconciles to active quote. |
| 21 | **Event "Total items value" row** | Total of all line items shown at the bottom of the event's Sub-events & items box. |
| 22 | **Create-new-client in New Event wizard** | Step 2 (Client & contact) now has a "+ New client" button + an empty-state "Create new client" prompt; opens the existing ClientForm in a modal, creates the client (same logic as the Clients module), and auto-selects it. |
| 23 | **Collapse superseded quote revisions** | Event detail (Documents → Quotations) and lead detail (Quotations panel) now show only non-superseded quotes by default; superseded revisions hide behind a "▸ Show N earlier revisions" toggle (expand/collapse in place). Presentational only — no query/data/active-quote logic changed. Rev number now shown on event-side rows too. _Tested OK by user._ |
| 25 | **Invoice Phase 3a — Invoices module + detail screen** | Sidebar **Invoices** is now a real module (was a stub): list with metrics (count / outstanding / received / overdue), search (ref/client/event) + status filter, rows → detail. **Invoice detail**: header (ref, status, client·event, "Go to event", quote-variance banner), **GST toggle** (pulls `settings.gst_pct`, recomputes tax+grand live, rescales installments, persists), line items grouped by sub-event, totals, installment schedule, received/outstanding, **Mark sent** (+auto due-date from `settings.default_invoice_due_days`) and **Cancel** actions (PDF stubbed for Phase 5). Wired nav: `navigate` carries `invoiceId`; event invoice rows now open the detail. No migration — code only. Built against `v22-db-schema.md` enums. **Refinements from testing:** (a) quote-vs-invoice variance now compares **pre-GST** (toggling GST no longer shows a false "difference"); (b) **Cancel** now frees the event — dedupe + event "Generate invoice" + payment-summary all ignore cancelled invoices, so a new invoice can be regenerated; (c) cancelled-invoice screen shows a hint on how to raise a new one. |
| 24 | **Invoice Phase 2 — auto-create from confirmed quote** | New `createInvoiceFromQuote()` helper + `getNextInvoiceRef()` (`I-YY-####`). A **draft** invoice is auto-created when a quote is confirmed: Path A (lead→event conversion) creates it at the end of conversion; Path B adds a **"✅ Confirm & create invoice"** action on the QuotationDetailModal for event-origin quotes (sets quote→approved, creates invoice, jumps to event). Event's Invoices box gains a **"+ Generate invoice" safety-net** (when a confirmed quote exists but no invoice) + shows received/rev; Payment summary now shows real Received/Outstanding. Fully-relational schema (line items, installments, payments). Guarded multi-table write (rolls back partial invoice on failure). **Requires the Phase 2 SQL migration (Section B-10).** |

---

## B. Database migrations run in Supabase (not in the HTML — separate from code deploys)

1. **`next_counter(p_type,p_year,p_seed)`** — atomic increment function (P0-4).
2. **`invoices`** — added `gst_applicable, gst_pct, tax_amount, discount_amount, due_date`.
3. **`settings`** — added `gst_pct` (default 18).
4. **`events`** — added `ref_number`; **`counters_type_check`** extended to include `'event'`; backfilled existing event refs; seeded event counter.
5. Renamed existing event refs `EV-` → `E-`; reset event counter so next = `E-26-1111`.
6. **Dropped `leads_source_check`** — custom lead sources were rejected (configurable `lead_sources` is now the source of truth).
7. **Storage**: created public bucket **`quotations`** + policy `quotations_all` (authenticated upload) — for hosted quote PDFs.
8. **`sub_event_items.sub_event_id`** — dropped NOT NULL (allows "main event" items not under a sub-event). _ISSUE-003._
9. Backfills: `quotations.event_id` from the lead's event (converted quotes); `events.lead_id` from `leads.event_id`.
10. **Invoice Phase 2 — RUN ✅ (13 Jun).** The `invoice_*` tables already existed (original schema) with their own column names — verified via `information_schema`. Revised migration ADDED: `invoices.revision_number`, `invoices.source_quote_total`; `invoice_line_items.is_deleted`; `invoice_installments.label / when_text / is_deleted`; + RLS policies + indexes. Confirmed present in the live schema dump. Code aligned to existing names (`total_received`/`total_outstanding`, `installment_number`/`percentage`, `client_name`/`event_name`). Script: `docs/v22-invoice-phase2-migration.sql`. Note: the 3 redundant `*_all` RLS policies my script added are duplicates of the pre-existing `*_policy` ones — optional cleanup SQL is in `v22-db-schema.md` §3.

> **Reminder:** the database and the code must stay in lock-step. If the app is ever restored from an older state, these migrations must already be present (they are, since they're applied to the live Supabase project).

11. **`invoice_activity_log` (Phase 3b) — PENDING USER RUN.** New table for the invoice audit/change-log timeline (sends now in 3b-i; revisions in 3b-ii): `log_id, invoice_id, revision_number, action, channel, field, old_value, new_value, reason, changed_by, changed_at` + RLS authenticated policy + index. Run before the 3b-i deploy or invoice send-logging silently no-ops (it's wrapped in try/catch). Quote logging uses the pre-existing `quotation_activity_log` (no migration).

12. **`invoice_payments.receipt_url` — PENDING USER RUN.** `ALTER TABLE invoice_payments ADD COLUMN IF NOT EXISTS receipt_url text;` — stores the URL of the optional payment-confirmation attachment (file goes to the existing `quotations` bucket, `receipts/` path; no new bucket). Run before testing the attachment.

14. **Configurable event types — PENDING USER RUN.** Creates the **`event_types`** table (label/value/sort_order/is_active, mirror of `lead_sources`), seeds the current five (wedding/corporate/birthday/anniversary/other), **drops `events_type_check`** so custom types are allowed, and adds an authenticated RLS policy + index. Script: `docs/v22-event-types-migration.sql`. Run before deploying changelog #42, or saving an event with a custom type (or any event save once the CHECK still exists alongside new types) could be rejected. Existing `events.type` values already match the seeded values (all writes are lowercased).

13. **Enum additions for the paid-cascade — PENDING USER RUN.** Adds two CHECK values: `quotations.status` gains **`'invoiced'`** (set when a tied invoice is fully paid) and `leads.stage` gains **`'completed'`** (set when the source event is marked completed). Script: `docs/v22-invoice-paidcascade-migration.sql` (drops + re-adds each `_check` constraint with the full existing value list + the new value). **Run before deploying changelog #40**, or the cascade's `UPDATE quotations SET status='invoiced'` and the mark-completed's `UPDATE leads SET stage='completed'` will be rejected by the CHECK and surface as toast errors. (`events.status` already allows `'completed'` — no change there.)

**⚠️ Large-file hazard (learned 14 Jun):** the sandbox/bash mount **truncates reads of large files** (the HTML is now ~600 KB after embedding the logo + Great Vibes font). A bash/python "read file → modify → write back" on the mount can read a *truncated* copy and write back a **truncated, corrupted file** (this happened once during the PDF splice and was caught + recovered). **Safe practice:** build any whole-file transform in `/tmp` sourced from `git show origin/main:...` (git reads are NOT truncated), transpile-verify the `/tmp` copy, then write it to the mount; never trust a bash read of the full file for length/markers. Per-region `Edit`-tool changes remain fine.

**Repo relocation (13 Jun 2026):** the working repo was moved **out of OneDrive** (`…\OneDrive\Documents\GitHub\…`) to **`C:\Users\vamsh\GitHub`** after OneDrive repeatedly corrupted the local `.git` (corrupt index, locked loose objects, failed pushes) and gave stale/truncated reads. A fresh clone was made and working files copied in; the post-fix Phase 3a edits were re-applied here (they'd been lost in the corrupt copy). **Do not move the repo back under OneDrive/any sync tool.** Files are now CRLF (a `.gitattributes` normalization is a small pending cleanup to stop whole-file diff churn).

---

## C. Open / pending items (to resolve before or as part of baselining)

**Functional gaps (from the spec):**
- Invoice module — Phases done: 2 (auto-create ✅), 3a (module + detail ✅), 3b (revise + change-log + send-audit + draft auto-refresh ✅), 5 (PDF + share ✅), 4 (record payments ✅ — pending live test). **Remaining:** A1 (installment due-dates + overdue auto-status); the post-baseline RPC pass for multi-table atomicity. **Remaining, in order:** **Phase 5** (invoice PDF + Email/WhatsApp/Print/Download — building next) → **⏳ Phase 3b (DEFERRED, MUST RETURN): revise invoice + change log + mismatch reasons + draft auto-refresh, needs new `invoice_activity_log` table)** → **Phase 4** (record payments + auto balance/status). Spec: `v22-invoice-spec.md`.
  - **Reminder for Claude:** after Phase 5 ships and is tested, proactively raise Phase 3b before anything else. It is the revise/audit work the user explicitly asked to be reminded about (decisions #7–#12 in the invoice spec).
- ~~Standalone **Quotations** module list~~ ✅ DONE (changelog #29).
- **Dashboard** real widgets (currently placeholder).
- **Users**, **Reports**, **Vendors**, **Vendor Payments**, **Expenses**, **Owner Account** (stubs).
- Template sub-event tagging (data task) for PDF grouping.

**Refinements raised during testing:**
- **Payment summary "Quoted (items)"** should reflect the **active quotation**, not the event's loose items (decide during Invoice build).
- **"Revise" button inside the quotation-detail modal** (deferred — only the event button was built).
- **"Go to event" hidden when already on that event** (small UX tweak, discussed, not built).
- ~~Long revision chains: optional "group/hide superseded" toggle in the quote lists.~~ ✅ DONE (changelog #23).

**Architecture / hardening (post-baseline):**
- **Transaction-wrap multi-table flows** (event save, lead→event conversion, quote-from-event + mirror) in Postgres RPCs so a mid-way failure can't leave partial/out-of-sync records.
- **P0-3 — real role resolution + role-aware RLS** (when staff accounts are added; today every authenticated user sees everything, incl. owner financials).
- **P2 performance/scale**: list pagination; precompile Babel + modularize (remove in-browser transpile); memoization; dedupe redundant queries.
- **Mobile (iOS/Android)**: PWA or Capacitor wrap — *after* the build-step graduation above.

**Security status:** RLS enabled on all tables; only authenticated users; **public sign-ups disabled** (verified). Solid baseline for a single/trusted-user setup.

**Test data cleanup:** delete throwaway test leads/events/quotes (e.g. "New Flow 2" partials, "Popup Test", "Test_Ref" events) before go-live.

---

## D. Companion documents in `/docs`
- `v22-functional-spec.md` — full functional spec + built/partial/pending status.
- `v22-invoice-spec.md` — Invoice module spec + phased plan.
- `v22-health-assessment.md` — code health assessment (P0–P3 findings).
- `v22-issues-log.md` — issues found in testing + their resolution.
- `isheeka-erp-complete-handoff.md`, `isheeka-v22-cowork-handoff.md` — original context handoffs.

---

## E. Pre-baseline review checklist (to do together before declaring v22 the baseline)
1. Walk this changelog end-to-end; confirm each shipped item still behaves.
2. Confirm all Section B migrations are present in the live Supabase project.
3. Resolve the "Open/pending" items we agree are baseline-blocking (vs. post-baseline).
4. Clean up test data.
5. Full regression smoke test of the core flow: Lead → Quote → (revise) → Convert → Event → Quote-from-event → Invoice (once built) → share/PDF.
6. Tag/snapshot the baseline (e.g. a dated commit / `v22-baseline`).
